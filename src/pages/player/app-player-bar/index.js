import React, { memo } from "react";

import { PlaybarWrapper } from "./style";

const WYPlayerBar = memo(() => {
  return <PlaybarWrapper className="sprite_player">WYPalyerBar</PlaybarWrapper>;
});

export default WYPlayerBar;
